<!DOCTYPE html>
<html>
  <head>
    <title>Crazyforcats</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
<nav class="navbar navbar-expand-lg bg-dark" >
  <div class="container ">
<i class="fa-solid fa-cat fa-2xl" style="color: #ffffff;"></i>
    <%= link_to "Inicio", root_path, class:"navbar-brand tcolor" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse " id="navbarNavDropdown">
    <ul class="navbar-nav me-auto">
    </ul>
      <ul class="navbar-nav ms-auto">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle tcolor" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <% if cfcuser_signed_in? %>
            <% if current_cfcuser.name %>
            Hola <%= current_cfcuser.name %>  
            <% else %>
            Hola <%= current_cfcuser.email %>
            <% end %>
            <% else %>
            Inicia sesión / Registrate
            <% end %>
          </a>
          <ul class="dropdown-menu">
          <% if cfcuser_signed_in? %>
            <li><%= button_to "Cerrar Sesión", destroy_cfcuser_session_path, class:"dropdown-item", method: :delete %></li>
            <% else %> 
            <li><%= link_to "Iniciar Sesión", new_cfcuser_session_path, class:"dropdown-item" %></li>
            <li><%= link_to "Registrarme", new_cfcuser_registration_path, class:"dropdown-item" %></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
<%# Creditos por imagen de fondo "https://www.freepik.es/vector-gratis/plantilla-fondo-estampado-amante-gatos_15080927.htm">Imagen de rawpixel.com en Freepik< %>

 
  <p class="notice"><%= notice %></p>
      <h1 class="tutilo"><u>~Crazy For Cats~</u></h1>
    <div class="row justify-content-center">

     <div class="col-6 text-center">
     
    <%= yield %>
</div>
</div>
    <script src="https://kit.fontawesome.com/8bcc232b0f.js" crossorigin="anonymous"></script>
  </body>
</html>